# SwiftForms Languages

This directory is reserved for generated translation files such as `swiftforms.pot`, localized `.po` files, and compiled `.mo` files.

Translation generation is not scaffolded yet, but the plugin is configured with the `swiftforms` text domain.